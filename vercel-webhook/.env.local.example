# Copy this to .env.local for local testing with `vercel dev`.
# In production, set these in Vercel Dashboard -> Project -> Settings -> Environment Variables.
# NEVER commit a file with real values in it.

# Paystack secret key (regenerate this — the original was pasted in chat)
PAYSTACK_SECRET_KEY=sk_test_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# From Firebase Console -> Project Settings -> Service Accounts -> Generate new private key
FIREBASE_PROJECT_ID=comzygold
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@comzygold.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nMIIEv...\n-----END PRIVATE KEY-----\n"
